version: '3.4'

services:
  backend:
    restart: on-failure
    build:
      context: ./materials-evaluation-dotnet/
    ports:
      - "5000:80"
    volumes:
      - ./materials-evaluation-dotnet:/App
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://*:80
  # FIXME: o angular não funciona para hot-reload nessas configurações
  # spa:
  #   restart: on-failure
  #   container_name: angular-ui
  #   build:
  #     context: ./materials-evaluation-spa/
  #   environment:
  #     CHOKIDAR_USEPOLLING: "true"
  #   ports:
  #     - 4200:4200
  #   volumes:
  #     - ./materials-evaluation-spa:/usr/src/app/web
  #     - /usr/src/app/web/node_modules
